{
    "compilerOptions": {
        "target":"ES6",
        "module":"commonjs",
        "jsx":"react",
        "baseUrl": ".",  // all paths are relative to the baseUrl
        "paths": {
            "~/*" : ["./app/*"]   // resolve any `~/foo/bar` to `<baseUrl>/foo/bar`
        }
    }
}