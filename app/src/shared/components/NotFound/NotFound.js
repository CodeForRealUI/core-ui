import React from 'react';
import PropTypes from 'prop-types';

function NotFound() {
  return (
    <div>
        Not Found Bro
    </div>
  );
}

NotFound.propTypes = {
};

export default (NotFound);
