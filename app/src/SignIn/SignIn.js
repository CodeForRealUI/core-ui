import React from 'react';
import PropTypes from 'prop-types';
import './styles.scss';

function SignIn() {
  return (
    <div className="sign-in" >
    sign in dude
    </div>
  );
}

SignIn.propTypes = {
};

export default (SignIn);
