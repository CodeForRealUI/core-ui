import React from 'react';
import PropTypes from 'prop-types';

import './styles.scss';

function NotFound() {
  return (
    <div className="test">
        Not Found Bro
    </div>
  );
}

NotFound.propTypes = {
};

export default (NotFound);
